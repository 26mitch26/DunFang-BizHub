# DunFang BizHub 项目技术白皮书

## 面向读者

本白皮书专为**校招求职者**和**技术小白**设计，旨在帮助你从零开始理解这个全栈项目的架构设计、技术选型和业务逻辑。无论你是准备面试还是学习全栈开发，这份文档都将是你的最佳指南。

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构详解](#2-技术架构详解)
3. [环境搭建与部署指南](#3-环境搭建与部署指南)
4. [核心模块深度解析](#4-核心模块深度解析)
5. [数据库设计与数据流转](#5-数据库设计与数据流转)
6. [面试高频问题与答案](#6-面试高频问题与答案)
7. [学习路径与进阶建议](#7-学习路径与进阶建议)

---

## 1. 项目概述

### 1.1 项目定位

**DunFang BizHub** 是一个面向中小型设备分销/贸易企业的**智能化管理平台**。想象一下：

- 一家卖热水器的贸易公司，需要管理多个分公司
- 需要处理大量销售订单
- 需要管理仓库库存
- 需要处理发票和财务结算

这个平台就是为解决这些问题而生的！

### 1.2 业务场景

| 角色 | 典型操作 |
|------|----------|
| 管理员 | 创建公司、管理用户权限 |
| 销售代表 | 创建订单、跟踪订单状态 |
| 仓库管理员 | 管理库存、处理入库出库 |
| 财务人员 | 处理发票、计算佣金 |

### 1.3 项目价值

对于求职者来说，这个项目展示了：
- ✅ 全栈开发能力（前端+后端）
- ✅ 企业级架构设计
- ✅ 业务逻辑实现
- ✅ 数据库设计能力

---

## 2. 技术架构详解

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层 (Frontend)                      │
│   React 18 + Ant Design Pro + TypeScript                   │
│   运行在: http://localhost:8000                            │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTP/REST API
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端层 (Backend)                       │
│   Spring Boot 3.4.5 + Spring Security + MyBatis-Plus       │
│   运行在: http://localhost:8080                            │
└───────────────────────────┬─────────────────────────────────┘
                            │ JDBC
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据层 (Database)                      │
│                   MySQL 8.0 (Docker容器)                    │
│                   端口: 3306                               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈详解

| 层级 | 技术 | 作用 | 类比 |
|------|------|------|------|
| 前端框架 | React 18 | UI组件开发 | 搭积木的框架 |
| 前端UI | Ant Design Pro | 现成的组件库 | 装修用的建材 |
| 前端语言 | TypeScript | 带类型的JavaScript | 更安全的编程语言 |
| 后端框架 | Spring Boot 3.4.5 | Java后端框架 | 快速建房子的脚手架 |
| 安全框架 | Spring Security | 用户认证授权 | 小区保安系统 |
| 数据库 | MySQL 8.0 | 数据存储 | 大型仓库 |
| ORM框架 | MyBatis-Plus | 数据库操作 | 仓库管理员 |
| 认证方式 | JWT | 无状态身份认证 | 电子门禁卡 |

### 2.3 JWT认证流程（小白版解释）

```
1. 用户登录 → 输入用户名密码 → 点击登录按钮
2. 前端发送请求到后端 /api/auth/login
3. 后端验证用户名密码
4. 验证成功 → 生成JWT令牌（一串加密字符串）
5. 后端返回令牌给前端
6. 前端保存令牌（存在浏览器里）
7. 以后每次请求 → 前端自动带上令牌
8. 后端验证令牌有效性 → 有效则处理请求
```

---

## 3. 环境搭建与部署指南

### 3.1 准备工作

在开始之前，请确保你的电脑安装了以下软件：

| 软件 | 版本要求 | 下载地址 |
|------|----------|----------|
| JDK | 21+ | https://www.oracle.com/java/technologies/downloads/ |
| Node.js | 18+ | https://nodejs.org/ |
| Maven | 3.8+ | https://maven.apache.org/download.cgi |
| Docker | 最新版 | https://www.docker.com/get-started |

### 3.2 启动步骤（Windows环境）

#### 第一步：启动数据库（使用Docker）

```bash
# 进入项目目录
cd E:\my_project\DunFang-BizHub

# 使用Docker Compose启动MySQL
docker-compose up -d
```

**解释**：这一步会启动一个MySQL数据库容器，相当于在你的电脑上开了一个数据库服务。

#### 第二步：启动后端服务

```bash
# 进入后端目录
cd dunfang-backend

# 启动Spring Boot应用
mvn spring-boot:run
```

**等待提示**：当你看到 "Started DunFangBizHubApplication" 时，说明后端启动成功了！

#### 第三步：启动前端服务

```bash
# 进入前端目录
cd dunfang-frontend

# 安装依赖（第一次运行需要）
npm install

# 启动开发服务器
npm run dev
```

**访问地址**：http://localhost:8000

### 3.3 登录系统

启动完成后，你可以使用以下账号登录：

| 邮箱 | 密码 | 角色 |
|------|------|------|
| admin@dunfang.com | 123456 | 管理员 |
| sales@dunfang.com | 123456 | 销售 |

### 3.4 常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 端口8080被占用 | 其他程序正在使用该端口 | 使用 `netstat -ano | findstr 8080` 找到进程，然后 `taskkill /F /PID 进程号` |
| Maven下载依赖慢 | 网络问题 | 使用 `mvn spring-boot:run -s settings-temp.xml` |
| 数据库连接失败 | MySQL容器未启动 | 先执行 `docker-compose up -d` |

---

## 4. 核心模块深度解析

### 4.1 模块架构图

```
┌────────────────────────────────────────────────────────────────┐
│                        系统架构                                │
├─────────────┬─────────────┬─────────────┬─────────────────────┤
│   公司管理   │   用户管理   │   订单管理   │      仓库管理       │
│  Company    │   User      │   Order     │     Warehouse      │
├─────────────┴─────────────┴─────────────┴─────────────────────┤
│                    统一认证层 (Security)                       │
├────────────────────────────────────────────────────────────────┤
│                    数据访问层 (MyBatis)                        │
└────────────────────────────────────────────────────────────────┘
```

### 4.2 公司管理模块

**功能说明**：管理多个租户公司，实现数据隔离。

**核心代码位置**：
- 控制器：`dunfang-backend/src/main/java/com/dunfang/bizhub/company/CompanyController.java`
- 服务层：`dunfang-backend/src/main/java/com/dunfang/bizhub/company/CompanyService.java`
- 数据层：`dunfang-backend/src/main/java/com/dunfang/bizhub/company/CompanyMapper.java`
- 实体类：`dunfang-backend/src/main/java/com/dunfang/bizhub/company/Company.java`

**关键设计**：使用**软删除**模式，删除时不会真正删除数据，而是标记`deleted=1`。

### 4.3 订单管理模块

**订单状态流转**：
```
草稿 → 确认 → 已发货 → 已完成
   ↑         ↓
  删除      取消
```

**核心业务逻辑**：
1. 创建订单时检查库存是否充足
2. 确认订单后自动扣减库存
3. 订单金额由后端重新计算，防止前端篡改

### 4.4 权限管理模块

**角色权限设计**：

| 角色 | 权限 | 可访问菜单 |
|------|------|------------|
| ADMIN | 全部权限 | 所有菜单 |
| SALES | 销售相关 | 订单管理、客户管理 |
| WAREHOUSE | 仓库相关 | 仓库管理、库存管理 |

**注解使用**：
```java
@PreAuthorize("hasRole('ADMIN')")  // 只有管理员能访问
@PreAuthorize("hasAnyRole('ADMIN', 'SALES')")  // 管理员或销售能访问
```

---

## 5. 数据库设计与数据流转

### 5.1 核心数据表

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| company | 公司信息 | id, name, status |
| sys_user | 用户信息 | id, email, password, company_id |
| sys_role | 角色信息 | id, name, code |
| sales_order | 销售订单 | id, company_id, customer_id, total_amount |
| product | 商品信息 | id, name, price, stock |
| warehouse | 仓库信息 | id, name, location |

### 5.2 数据关联关系

```
company (公司) 1 ──── * sys_user (用户)
company (公司) 1 ──── * sales_order (订单)
sales_order (订单) 1 ──── * sales_order_item (订单项)
product (商品) 1 ──── * sales_order_item (订单项)
warehouse (仓库) 1 ──── * product (商品库存)
```

### 5.3 多租户数据隔离

**设计原则**：每个用户只能看到自己公司的数据。

**实现方式**：
1. 在实体类中添加 `companyId` 字段
2. 使用 MyBatis-Plus 的租户拦截器自动添加公司ID条件
3. 在 `MybatisPlusConfig.java` 中配置忽略的表（如系统角色表）

---

## 6. 面试高频问题与答案

### 6.1 技术问题

**Q1：什么是JWT？为什么要用JWT？**

A：JWT是JSON Web Token的缩写，是一种无状态的身份认证方式。
- 传统方式：每次请求都要查询数据库验证用户
- JWT方式：登录时生成一个包含用户信息的令牌，后续请求只需验证令牌签名即可
- 优点：无状态、可扩展、跨域友好

**Q2：什么是软删除？为什么要用软删除？**

A：软删除不是真正删除数据，而是标记删除状态（如`deleted=1`）。
- 优点：可以恢复误删数据、保留历史记录、便于审计
- 实现：使用MyBatis-Plus的`@TableLogic`注解

**Q3：Spring Boot的启动流程是什么？**

A：
1. 加载主类，启动Spring应用上下文
2. 扫描@Component注解，注册Bean
3. 自动配置（AutoConfiguration）
4. 初始化数据库连接、Redis等
5. 启动内嵌服务器（Tomcat）
6. 监听指定端口，等待请求

**Q4：什么是RESTful API？设计原则是什么？**

A：REST是一种软件架构风格。
- 使用HTTP方法表示操作：GET（查询）、POST（创建）、PUT（更新）、DELETE（删除）
- 无状态：每个请求都包含完整信息，服务器不保存会话状态
- 统一接口：使用标准的HTTP状态码

**Q5：如何保证接口的安全性？**

A：
1. 使用JWT进行身份认证
2. 使用Spring Security进行权限控制
3. 对敏感接口添加角色校验（@PreAuthorize）
4. 使用HTTPS传输数据
5. 接口参数校验（@Valid）

### 6.2 业务问题

**Q6：订单创建时需要做哪些校验？**

A：
1. 验证用户是否登录
2. 验证商品是否存在
3. 验证库存是否充足
4. 验证订单金额计算正确
5. 验证用户所属公司是否有权限

**Q7：如果订单确认后库存不足怎么办？**

A：
1. 在确认订单时再次检查库存
2. 如果库存不足，返回错误提示
3. 可以考虑使用分布式锁防止超卖
4. 或者使用乐观锁（version字段）

**Q8：如何实现多租户数据隔离？**

A：
1. 在每个业务表添加company_id字段
2. 使用MyBatis-Plus的租户拦截器自动注入条件
3. 在查询时自动过滤当前用户所在公司的数据
4. 系统表（如角色表）不需要隔离，配置忽略列表

---

## 7. 学习路径与进阶建议

### 7.1 入门阶段（1-2周）

1. **环境搭建**：学会安装JDK、Node.js、Maven、Docker
2. **项目启动**：按照文档成功启动前后端
3. **熟悉业务**：浏览系统，了解各个模块的功能
4. **代码阅读**：从Controller层开始，理解请求处理流程

### 7.2 进阶阶段（2-4周）

1. **深入后端**：
   - 学习Spring Boot核心注解（@Controller, @Service, @Repository）
   - 理解Spring Security和JWT认证流程
   - 学习MyBatis-Plus的使用

2. **深入前端**：
   - 学习React组件开发
   - 理解Ant Design Pro的布局和路由
   - 学习TypeScript类型系统

3. **数据库学习**：
   - 理解数据库设计原则
   - 学习SQL语句优化
   - 理解索引的作用

### 7.3 高级阶段（4周+）

1. **性能优化**：
   - 数据库查询优化
   - 缓存策略（Redis）
   - 接口响应时间优化

2. **架构设计**：
   - 微服务架构理解
   - 分布式事务处理
   - 消息队列应用

3. **安全加固**：
   - SQL注入防护
   - XSS攻击防护
   - CSRF防护

### 7.4 面试准备清单

- ✅ 熟悉项目整体架构
- ✅ 能讲解核心模块的业务流程
- ✅ 能解释关键技术点（JWT、软删除、多租户）
- ✅ 能回答常见面试问题
- ✅ 准备好项目演示（提前启动，确保能正常运行）

---

## 附录：项目文件结构

```
DunFang-BizHub/
├── dunfang-backend/                    # 后端项目
│   ├── src/main/java/com/dunfang/bizhub/
│   │   ├── company/                    # 公司管理模块
│   │   ├── sales/                      # 销售订单模块
│   │   ├── warehouse/                  # 仓库管理模块
│   │   ├── customer/                   # 客户管理模块
│   │   ├── security/                   # 安全认证模块
│   │   ├── config/                     # 配置类
│   │   └── common/                     # 通用工具类
│   └── src/main/resources/
│       ├── db/migration/               # 数据库迁移脚本
│       └── application.yml             # 应用配置
├── dunfang-frontend/                   # 前端项目
│   └── src/
│       ├── pages/                      # 页面组件
│       ├── services/                   # API服务
│       ├── components/                 # 通用组件
│       └── layouts/                    # 布局组件
├── docker-compose.yml                  # Docker配置
└── README.md                           # 项目说明
```

---

## 结语

这份白皮书希望能帮助你快速理解DunFang BizHub项目。记住，学习编程最重要的是实践！多阅读代码、多调试、多提问，你一定能掌握全栈开发的技能。

祝你求职顺利！🚀
