import { PageContainer } from '@ant-design/pro-components';
import React from 'react';
import { Card } from 'antd';

const GiftingCalendarPage: React.FC = () => {
  return (
    <PageContainer>
      <Card>
        <div>节日日历配置与提醒 (正在开发中...)</div>
      </Card>
    </PageContainer>
  );
};

export default GiftingCalendarPage;
